import { writable } from "svelte/store";
export const UnitData = writable([]);
export const UnitSettings = writable([]);
export const UnitLocations = writable([]);
export const RaidableUnits = writable([]);
export const LocaleSettings = writable([]);
